{
  "name": "habit-your-way",
  "version": "1.0.0",
  "description": "Track your ways into a Habit",
  "main": "server.js",
  "scripts": {
    "start": "node server/server.js",
    "develop": "concurrently \"cd server && npm run watch\" \"cd client && npm run dev\"",
    "install": "cd server && npm i && cd ../client && npm i",
    "build": "cd client && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@apollo/server": "^4.7.1",
    "apollo-server-express": "^3.12.0",
    "bcryptjs": "^2.4.3",
    "cloudinary": "^1.40.0",
    "concurrently": "^8.2.0",
    "express": "^4.18.2",
    "graphql": "^16.8.0",
    "mongodb": "3.6",
    "multer": "^1.4.5-lts.1"
  }
}
